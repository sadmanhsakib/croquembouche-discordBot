name: Deploy to WispByte

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Confirm deployment
        run: echo "Deployment recorded for WispByte"